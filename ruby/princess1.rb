#!/usr/bin/ruby
def nextMove(n,r,c,grid)
  puts "weee"
  puts "0 0"
end
n = gets.to_i

puts "N = #{n}"
r,c = gets.strip.split.map {|num| num.to_i}

grid = Array.new(n)

(0...n).each do |i|
    #grid[i] = gets

   puts i
end

puts "N is #{n}"

#nextMove(n,r,c,grid)

