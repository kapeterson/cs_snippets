#include <stdio.h>
#include <omp.h>
int main() {
	int nthreads, tid;

	printf("Hello");
	#pragma omp parallel private(tid)
	{
		tid = omp_get_thread_num();
		printf("Hello from thread %d\n",tid);

	}
}
